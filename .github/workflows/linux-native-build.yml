name: Build Linux Native

on:
  push:
    branches: [ main, develop ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '25'
          distribution: 'graalvm-community'
          components: 'native-image'
          github-token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Run Tests
        # Citattecken runt -D parametrar för att undvika tolkningsfel
        run: mvn test -Pci "-Dtest.threshold=0.3" "-Dtest.envFactor=10"
        
      - name: Build Native Image
        run: mvn package -Pnative -DskipTests=true

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: hedgehog-linux
          # GraalVM på Linux skapar binären utan filändelse
          path: native-image/target/hedgehog
          
          
          
